HOLA

// Declaración de variables
entero = x;
entero = y;
booleano = esMayor;

// Asignación
x = 5;
y = 10;

// Condicional
si (x < y) entonces {
    esMayor = falso;
} sino {
    esMayor = verdadero;
}

// Ciclo repetitivo
mientras (x < y) {
    x = x + 1;
}

ADÍOS
